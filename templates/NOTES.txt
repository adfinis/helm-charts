1. In Helm Version <2.7 following job needs to be cleaned manually.

{{- if .Release.IsUpgrade }}
  kubectl delete job post-upgrade-{{ template "timed.fullname" . }}-dbmigrate
{{- end }}
{{- if .Release.IsInstall }}
  kubectl delete job post-install-{{ template "timed.fullname" . }}-dbmigrate
{{- end }}

2. Open following URL to start using Timed:
{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  http://{{ . }}
{{- end }}

3. Login with user `{{ .Values.backend.service.adminUsername }}`.

Passwort you can retrieve with:
  kubectl get secret pre-install-{{ template "timed.fullname" . }}-backend -o yaml | awk '/DJANGO_ADMIN_PASSWORD/{print $NF}' | echo $(base64 --decode)

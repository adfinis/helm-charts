chart-repos:
  - adfinis=https://charts.adfinis.com
  - bitnami=https://charts.bitnami.com/bitnami
  - secrets-store-csi-driver=https://raw.githubusercontent.com/kubernetes-sigs/secrets-store-csi-driver/master/charts
excluded-charts:
  - common
  - argoconfig
  # the huawei-csi-plugin is currently excluded
  # because the elevated privileges (p.e. Host-
  # Path directories) are not available in our
  # kind testing cluster
  - huawei-csi-plugin

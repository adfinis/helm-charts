{{- if and .Values.monitoring.enabled (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") }}
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "openshift-etcd-backup.fullname" . }}-cronjob-monitor
spec:
  groups:
  - name: openshift-etcd-backup
    rules:
{{- tpl .Values.monitoring.rules . | nindent 4 }}
{{- end }}

suite: clusterrolebinding tests
templates:
  - clusterrolebinding.yaml
tests:
  - it: should match snapshot
    values:
      - ../ci/default-values.yaml
    asserts:
      - hasDocuments:
          count: 1
      - matchSnapshot: {}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "back8sup.fullname" . }}
  labels:
    {{- include "back8sup.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

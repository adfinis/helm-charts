minio:
  enabled: true
  project: infra-storage
  values:
    mode: distributed
    replicas: 4
    persistence:
      enabled: true
      storageClass: default
      size: 10Gi
    resources:
      requests:
        memory: 2Gi

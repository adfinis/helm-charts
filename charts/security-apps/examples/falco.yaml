falco:
  enabled: true
  project: infra-falco
  values:
    services:
     - name: k8saudit-webhook
       ports:
         - port: 9765
           protocol: TCP
    falcosidekick:
      enabled: true

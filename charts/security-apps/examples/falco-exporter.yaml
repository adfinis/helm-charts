falcoExporter:
  enabled: true
  project: infra-falco
  values:
    grafanaDashboard:
      enabled: true
      additionalLabels:
        k8s.adfinis.com/prometheus: kube-prometheus
    serviceMonitor:
      enabled: true
    prometheusRules:
      enabled: true

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "customerCenter.fullname" . }}-frontend
  labels:
    {{- include "customerCenter.labels" . | nindent 4 }}
    app.kubernetes.io/component: timed
data:
  OIDC_CLIENT: {{ .Values.auth.oidcClient | quote }}

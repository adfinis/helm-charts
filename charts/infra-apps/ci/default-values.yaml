argocd:
  enabled: true
  values:
    server:
      ingress:
        annotations:
          k8s.adfinis.com/test-annotation: test
argocdNotifications:
  enabled: true
  values:
    TODO: add values in values.yaml
certManager:
  enabled: true
  values:
    TODO: add values in values.yaml
certManagerIssuers:
  enabled: true
  values:
    TODO: add values in values.yaml
ingressNginx:
  enabled: true
  values:
    TODO: add values in values.yaml
rbacManager:
  enabled: true
  values:
    TODO: add values in values.yaml
thanos:
  enabled: true
  values:
    persistence:
      enabled: false
kured:
  enabled: true
kubeEventExporter:
  enabled: true

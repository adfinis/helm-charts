secretsStoreCsiDriverAzure:
  enabled: true
  project: infra-secrets-store-csi-azure
  values:
    # Install RBAC roles and bindings required for K8S Secrets syncing if true
    syncSecret:
      enabled: true
    enableSecretRotation: true
